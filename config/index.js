/**
 * @author : Manjeet Kumar
 * @description : simple config for storing certain configurations for the API.
*/

let config = {
    dbURL: "mongodb://localhost/hrg_authAPI",
    secret: "chelsea"
}

module.exports = config;